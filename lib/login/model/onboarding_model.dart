import 'dart:ui';

class OnBoardingInfo {
  dynamic imageAsset;
  final Color color;
  dynamic title;
  dynamic description;

  OnBoardingInfo(this.imageAsset, this.title, this.description,this.color);
}